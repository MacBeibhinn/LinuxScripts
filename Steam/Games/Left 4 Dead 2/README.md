# Installation

Scripts may need permission to be executed, set a shortcut in your keyboard to run the command `./bunny_hop.sh` or `/home/username/bunny_hop.sh`. The scripts act as a toggle, if the script has been triggered and is already active or you switch window then the script will stop, otherwise it will continue.
